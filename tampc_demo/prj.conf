# Logging
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3

# GPIO (for LED and pin configuration)
CONFIG_GPIO=y

# TrustZone: build as secure-only (TAMPC is secure-only peripheral)
CONFIG_TRUSTED_EXECUTION_NONSECURE=n
